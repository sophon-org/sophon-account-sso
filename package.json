{
  "name": "sophon-sso-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:connector": "npm run build --workspace=packages/connector",
    "dev:auth": "npm run dev --workspace=packages/auth-server",
    "dev:demo": "npm run dev --workspace=packages/demo-app",
    "build:auth": "npm run build --workspace=packages/auth-server",
    "dev:connector": "npm run dev --workspace=packages/connector",
    "build:all": "npm run build --workspaces",
    "install:all": "npm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
