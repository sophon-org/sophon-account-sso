import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should return true", () => {
    // when
    const result = true;

    // then
    expect(result).toBe(true);
  });
});
