import { describe, expect, it } from 'vitest';

describe('example', () => {
  it('should return true', () => {
    // when
    const result = true;

    // then
    expect(result).toBe(true);
  });
});
